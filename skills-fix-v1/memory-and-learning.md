---
name: "memory-and-learning"
description: "Durable memory, learning preferences, teaching any topic."
---

# Memory & Learning

**LUXOR-IMPROVED · 2026-09-21** — Luxor upgrade: added "Christian's setup" and "Lessons baked in" sections; every original line kept intact, zero lines removed. This file is one unit (batch-03: notes/knowledge/outlook).


- MEMORY.md is the business brain: every durable fact, decision, preference, and commitment gets recorded before replying.
- Search memory before answering anything about prior work, decisions, dates, people, or preferences. Never fabricate.
- Learn preferences from Gmail — findings come back for review before anything is saved.
- Teach or explain any topic in depth. Talk up: simple words are respectful, simplified ideas are not.

---

## Christian's setup

- Standing order (2026-09-20): `MEMORY.md` is the business brain — keep it
  constantly updated; every durable fact, decision, preference, and
  commitment gets recorded as it happens, before replying.
- The memory system: curated long-term memory in `~/memory/...`; daily notes
  at `~/memory/YYYY-MM-DD.md`; people pages under `~/memory/people/` (with
  `INDEX.md`); groups under `~/memory/groups/` (with `INDEX.md`).
- Memory files are READ-ONLY for the agent: read via `muse.memory_search` /
  `muse.memory_get` (and `muse.memory_explain` when he asks how a memory was
  learned or wants one corrected). Never edit them directly — record findings
  in the final report.
- Gmail personalization learning (approved 2026-09-20): findings come back
  to Christian for review before anything is saved. The learn-from-Gmail
  widget yes/no is still pending his answer.
- People pages are canonical for relationships: check the page before acting
  or writing about someone; never contradict it unless newer information
  turned up. Example: `~/memory/people/bridge28.md` — his AI worker on
  luxucleen.com, run by Luxor as CEO.
- Teaching rule from the original: talk UP — simple words are respectful,
  simplified ideas are not.

## Lessons baked in

Real lessons from actual operation:

- Never fabricate a fact — search memory first (original line, reinforced by
  the correction history: Christian corrects fast when something is off).
- Memory is the business brain only if it stays current: record durable
  facts as they happen, before replying, not at end-of-day.